numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
onnx>=1.15
onnxruntime>=1.16   # opcional (apenas para verificar o .onnx)
requests>=2.28      # download do xeno-canto (fetch_xenocanto.py)
# ffmpeg (binario no PATH) e necessario para decodificar os MP3
# (build_real_dataset.py) — nao e um pacote pip.
